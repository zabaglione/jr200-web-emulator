<!-- jr200-plan:P02 -->
# P02 — CJR構造・検証・損失なし保存・BIN包装

## 目的
公開コードに基づいたCJRを安全に読み書きし、後続のエミュレータから再利用する。

## 依存
{{P01}}

計画IDはGitHub Issue番号ではない。登録時に実際の番号へ置き換える。

## 現時点の状態
2026-09-22受入完了。合成データによるnative/WASM/CLI/ブラウザUIを確認した。独立した既存実装との互換はP03。

## 作業
- [x] ヘッダー/データ/フッター、checksum、長さ00=256を実装する
- [x] BASIC/マシン語、baud生値、16生バイト名、複数アドレスを保持する
- [x] 破損入力のoffsetと診断、CLI、ブラウザ検査UIを実装する

## 受入条件
- [x] 手計算goldenと1/255/256/257/512等の境界に一致する
- [x] 切断/overflow/破損で領域外アクセスや無言の修復がない
- [x] 同じコアのnative/WASM出力が一致し、元ファイルを損なわない

## 受入記録（2026-09-22）

- `build/native/test_cjr`: 24テスト群成功。手計算47バイトgolden、1/255/256/257/511/512/513/65024境界、全切断位置、checksum、overflow、未知値、生メタデータ、疎なアドレス、失敗時の非更新、12,000件の決定的変異入力を含む。
- `make sanitize`: ASan/UBSan構成でCTest 3/3成功。検出された領域外アクセス・未定義動作なし。
- `WASM_CXX=/opt/homebrew/opt/emscripten/libexec/llvm/bin/clang++ make wasm-smoke`: nativeと同じ手計算golden、8境界長、破損拒否、C ABI引数検査が成功。JS wrapperのinspect/pack/出力コピー/診断/入力検査も成功。
- `cjrtool`でプロジェクトの`LICENSE`を合成payloadとしてpack→inspect→copy→extract。元payloadとextract、元CJRとcopyはそれぞれ`cmp`一致。7ブロック、payload 1755バイト、警告0を観測。
- 生成したClang直接WASM配布物をlocalhostで配信し、Codex in-app browserで`WASM起動済み / 処理はローカルのみ`、エミュレータ未実装表示、検査/作成UI、入力未選択時のエラーを目視。ブラウザconsoleのerror/warningは0件。
- GitHub Actions run 35620154336: commit `f9dbe6ac722e09dcaa612dc4d51209dc6fb1a44f`に対するLinux/macOS native、sanitizer、直接WASMの4ジョブが成功。

ブラウザでのファイル選択・ダウンロード自動試験は現在のPython環境にPlaywrightがなく未実行である。Node wrapper試験とブラウザ起動確認を合わせても、独立したWindows実装、実機CJR、通常カセットLOAD/SAVE、WAV互換の証拠にはならない。これらを本Issueの合格に含めず、P03以降に残す。

## 成果物と証拠
コード、関連テスト、実行コマンド、version/commit、観測結果、未検証範囲を記録する。Issueの要件が満たされた後にのみcloseする。ローカルの一部実装はこのIssue全体の完了と同一ではない。

## 範囲外
本Issueの目的以外の周辺機能、public化、ROM/メーカー由来フォント/商用テープの同梱。依存元の未合格条件を無言で飛ばさない。

共通仕様: docs/SOW.md / docs/UPSTREAM.md / AGENTS.md
