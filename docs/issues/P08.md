<!-- jr200-plan:P08 -->
# P08 — CJRカセットLOAD/MLOAD/SAVE/MSAVE統合

## 目的
CJRとエミュレータの通常カセット経路を結び付ける。

## 依存
{{P07}}

計画IDはGitHub Issue番号ではない。登録時に実際の番号へ置き換える。

## 現時点の状態
2026-09-22受入完了。CJRの信号生成とMN1271の通常read/write経路を接続し、
実ROMのLOAD/MLOAD/SAVE/MSAVEで確認した。実装commit
`a0cc4c6a96b4c993d2d5bfc84f3280d75dd15426`のActions run `35657825103`は
4ジョブすべてsuccess。詳細は
[P08_CASSETTE_ACCEPTANCE.md](../P08_CASSETTE_ACCEPTANCE.md) に記録した。

## 作業
- [x] mount/eject/rewindとREMOTE/read/write状態遷移を実装する
- [x] CJRからシリアル/信号を投入し、SAVE/MSAVEからCJRを生成する
- [x] 高速RAM注入は実装せず、通常信号経路だけをUIへ公開する

## 受入条件
- [x] BASIC/マシン語が通常LOAD/MLOADで動作する
- [x] SAVE/MSAVE出力を再読込し、論理領域・内容が一致する
- [x] CJRで表現しきれないPRINT#/INPUT#や特殊形式を黙って成功にしない

## 受入記録（2026-09-22）

- native/sanitizerで2400/600 baud、transport状態、固定容量、特殊type拒否を確認した。
- 実ROMの`SAVE`→`LOAD`で2行のBASICと`RUN`結果`42`が一致した。
- 実ROMの`MSAVE`→`MLOAD`で`$7000`〜`$7003`の4 byteが一致した。
- 実機MSAVE録音から独立ツールで復元したfont CJRも通常`MLOAD`し、変更済み領域が
  CJR内容へ戻った。WAV復元そのものはP11、本実装と物理実機の往復はP12に残る。

## 成果物と証拠
コード、関連テスト、実行コマンド、version/commit、観測結果、未検証範囲を記録する。Issueの要件が満たされた後にのみcloseする。ローカルの一部実装はこのIssue全体の完了と同一ではない。

## 範囲外
本Issueの目的以外の周辺機能、public化、ROM/メーカー由来フォント/商用テープの同梱。依存元の未合格条件を無言で飛ばさない。

共通仕様: docs/SOW.md / docs/UPSTREAM.md / AGENTS.md
