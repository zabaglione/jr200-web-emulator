<!-- jr200-plan:P01 -->
# P01 — C++20共通コア・native/WASMビルド基盤

## 目的
ホスト依存を持たない共通コアと明示的C ABI、native/直接WASMの初期ビルド基盤を用意する。正式Emscriptenルートの実行確認はP06、remote CIと再現性の最終判定はP13で行う。

## 依存
{{P00}}

計画IDはGitHub Issue番号ではない。登録時に実際の番号へ置き換える。

## 現時点の状態
nativeとClang WASMをローカル検証。Emscriptenとremote CIは未実行。

## 作業
- [ ] CMake/Make、C ABI、ネイティブテスト、ASan/UBSanを整備する
- [ ] Clangによる依存なしWASM smokeとEmscriptenルートを分けて記録する
- [ ] Linux/macOS/native/WASMのGitHub CI定義を用意する（実行済みとは表示しない）

## 受入条件
- [ ] native test、sanitizer、WASM smokeが成功する
- [ ] Emscripten用のCMake/JS設定を用意し、未実行の受入ゲートをP06に明記する
- [ ] CI定義を用意し、remote CI未実行を記録する。実行証拠の最終判定はP13

## 成果物と証拠
コード、関連テスト、実行コマンド、version/commit、観測結果、未検証範囲を記録する。Issueの要件が満たされた後にのみcloseする。ローカルの一部実装はこのIssue全体の完了と同一ではない。

## 範囲外
本Issueの目的以外の周辺機能、public化、ROM/メーカー由来フォント/商用テープの同梱。依存元の未合格条件を無言で飛ばさない。

共通仕様: docs/SOW.md / docs/UPSTREAM.md / AGENTS.md
